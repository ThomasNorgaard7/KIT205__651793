#include <stdio.h>
#include "graph.h"

int main() {
    // Declare a variable of type Graph
    Graph G;

    // You can initialize the graph here, load the graph data, or perform other tasks

    return 0;
}